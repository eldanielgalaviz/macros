// src/app/models/user-details-response.model.ts

export interface UserDetailsResponse {
    cantidad_comidas: number;  // Aquí defines las propiedades que esperas recibir en la respuesta de la API.
    // Puedes agregar más campos si es necesario:
    // nombre: string;
    // correo: string;
    // rol: string;
  }
  